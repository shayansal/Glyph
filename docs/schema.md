# Schema

Schemas live in `schemas/` and can be regenerated with:

```bash
cargo run -p glyphspace-cli -- export-schema --out schemas
```

The prototype uses serde and schemars for the reference schema shape.

