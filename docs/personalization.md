# Personalization

Personalization is stored as reversible patches. Patch layering order is base app, organization policy, role lens, user lens, session state, and temporary AI suggestions.

Patch operations are validated before application. Unsafe operations are rejected before layout or rendering.

